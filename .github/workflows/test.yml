name: Test
on:
  push:
    paths:
      - '**.go'
      - 'go.*'
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: ./go.mod
      - name: Test
        run: |
          make test
      - name: Vet
        run: |
          make vet
